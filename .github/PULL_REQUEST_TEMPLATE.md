## 变更说明

<!-- 简要说明本次 PR 的目的和主要改动 -->

## 变更类型

- [ ] 缺陷修复（不会破坏现有功能的修复）
- [ ] 新功能（不会破坏现有功能的新增）
- [ ] 重大变更（可能导致现有功能变化）
- [ ] 文档更新

## 如果是新的云厂商适配器

- [ ] 实现了 `ICloudProvider` 接口的所有方法
- [ ] `GetCredentialFields()` 已声明所有凭证字段
- [ ] 添加了 `[Service]` 特性（自动注册）
- [ ] 更新了 README.md 的厂商支持列表
- [ ] 添加了基本单元测试

## 安全检查

- [ ] 代码中没有真实的 API Key 或密码
- [ ] 没有新增日志输出凭证信息的代码
- [ ] 凭证处理代码均通过 `CredentialEncryptionService`

## 测试

- [ ] 本地 `dotnet build` 和 `dotnet test` 通过
- [ ] 相关测试用例已添加或更新
- [ ] 手动测试过主要功能路径
